name: Update CRAN download statistics

on:
  schedule:
    - cron: "20 8 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-cranlogs:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up R
        uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      - name: Install R packages
        uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: |
            any::cranlogs
            any::jsonlite
            any::httr2
          needs: |
            cranlogs
            jsonlite
            httr2

      - name: Check package installation
        run: |
          packageVersion("cranlogs")
          packageVersion("jsonlite")
          packageVersion("httr2")
        shell: Rscript {0}

      - name: Update README statistics
        env:
          GH_TRAFFIC_TOKEN: ${{ github.token }}
        run: Rscript update_cranlogs_readme.R

      - name: Commit and push changes
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          git diff --cached --quiet || git commit -m "Update CRAN download statistics"
          git push
